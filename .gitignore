# секреты — в репозиторий не заливать
data/
*.session
*.session-journal
.env

# состояние рассылки ведётся в корне и коммитится воркфлоу GitHub Actions
# (leads.json, processed.txt, archived.txt, city_cursor.txt)

__pycache__/
*.pyc
leads.html
input.txt
message.txt
